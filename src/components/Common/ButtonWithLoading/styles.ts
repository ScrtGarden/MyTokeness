import styled from 'styled-components'

import Dots from '../../UI/Loaders/Dots'

const StyledDots = styled(Dots)`
  bottom: 0;
  left: 0;
  margin: auto;
  position: absolute;
  right: 0;
  top: 0;
  width: 100%;
  height: 100%;
`

export { StyledDots }
