import { Container, Content, InnerContainer } from '../../UI/Containers'
import { PageTitle } from '../../UI/Typography'

const Burn = () => {
  return (
    <Container>
      <InnerContainer>
        <PageTitle>Burn</PageTitle>
        <Content></Content>
      </InnerContainer>
    </Container>
  )
}

export default Burn
